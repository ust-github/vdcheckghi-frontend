pipeline {
  agent { label 'chrome' }
  tools {
    nodejs 'NodeJS'
  }
  stages {
    stage ('Installing (wxnefw)') {
      steps {
        checkout scm
      } //end steps
    } // end stage Installing
  }
}
